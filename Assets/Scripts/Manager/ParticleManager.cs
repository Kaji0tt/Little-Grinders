using System.Collections;
using System.Collections.Generic;
using UnityEngine;
/*
public static class ParticleManager 
{

    public static void SetParticleStartColor(GameObject go, Color color)
    {
        ParticleSystem ps = go.GetComponent<ParticleSystem>();
        ParticleSystem.MainModule mainModule = ps.main;
        mainModule.startColor = color;
    }

    public static void SetParticleGradient(GameObject go, Color[] color, float[] alphas)
    {
        ParticleSystem ps = go.GetComponent<ParticleSystem>();

        Gradient grad = new Gradient();
        grad.SetKeys(
            for(int i = 0; i <= color.Length; i++)
            new GradientColorKey[] { new GradientColorKey(color[i])}, 
            new GradientAlphaKey[] { new GradientAlphaKey(0.0f, 0.0f), new GradientAlphaKey(1.0f, 0.7f), new GradientAlphaKey(0f, 1.0f) });


    }

}
*/
