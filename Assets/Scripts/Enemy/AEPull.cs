using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AEPull : MonoBehaviour
{
    /*
    private void OnTriggerStay(Collider collider)
    {

        if (collider.gameObject.tag == "AE-Pull")
        {
            print("got collision");
            EnemyController groupPulled = collider.GetComponentInParent<EnemyController>();
            if (groupPulled.pulled)
            {
                EnemyController thisMob = GetComponentInParent<EnemyController>();
                thisMob.pulled = true;
                print("got pulled");
            }
        }
    }
    */

    //Needs better workaround.

}
